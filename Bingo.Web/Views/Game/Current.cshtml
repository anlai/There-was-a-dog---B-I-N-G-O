@model Bingo.Web.Controllers.GameViewModel
@using Bingo.Web.Helpers;

@{
    ViewBag.Title = "Current Game";
}

<h3>Welcome to the Bingo admin page!</h3>
<p>&nbsp;</p>

@using (Html.BeginForm("PickNewNumber", "Game", FormMethod.Post))
{
    @Html.AntiForgeryToken()
    <input type="submit" value="Pick New Number!" />
}

<br />
<p>Numbers already called:</p>
@if (Model.Game.CalledNumbersArray.Count > 0)
{
<ul>
    @foreach (var ball in Model.Game.CalledNumbersArray)
    {
        <li>Ball #@ball.ToBingoBall()</li>
    }
</ul>
}
else
{ 
    <text>No balls have been picked yet</text>
}